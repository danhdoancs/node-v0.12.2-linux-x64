function getShortMessages(messages) {
    var newMessages = messages.filter(function(msg,index,arr){
        return (msg.message.length < 50)
    });

    return newMessages.map(function(msg){
        return msg.message;
    });
}

module.exports = getShortMessages;
